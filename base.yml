- hosts: servers
  tasks:
    - name: Debug SSH key file path
      command: ls -l /home/ubuntu/.ssh/id_rsa
      delegate_to: localhost

    - name: Transfer file from ServerA to ServerB
      synchronize:
        src: /home/ubuntu/sanket
        dest: /home/ubuntu/sanket
      delegate_to: 172.31.17.157
